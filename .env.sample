# ===== CONTOH ENVIRONMENT VARIABLES =====
# Ini adalah CONTOH format saja
# File ini untuk referensi, jangan gunakan di production
# Ganti dengan nilai sebenarnya di .env file

PORT=3001
NODE_ENV=development

# Contoh MongoDB URI (GANTI dengan URI asli Anda)
MONGO_URI=mongodb+srv://username:password@cluster-name.mongodb.net/barberflow?retryWrites=true&w=majority

# Contoh Email Credentials (GANTI dengan email asli Anda)
EMAIL_PENGIRIM=example@gmail.com
PASSWORD_EMAIL=example-app-password-16-chars

# Optional configs
ALLOWED_ORIGINS=*
API_TIMEOUT=30000

